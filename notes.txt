+ Exit gracefully when exception thrown (e.g. infinite switch mode loop)
+ IndicatorManager should listen for mouse position and give it to WindowsIndicator
+ Update hint windows when screens changed?
+ Hint undo key could be a combo instead of a key
+ Rename Mouse, Wheel, etc. to Mouse/Wheel/...Configuration
+ mode.blinking-mouse.enabled/visible-duration/invisible-duration
++ Zoom mode? Useful for 2-stage hint?
+ Configuration for indicator offset
+ Improve error handling in config parser
 + Wrap all parseBoolean/... into try catch that log the error with the property key and value

+ Check if the preparation is still a prefix of at least one combo.
 + If it is not, then it means a key is being pressed for longer than what the combo expects,
 + and the key can be regurgitated (just like it is regurgitated upon key release).
 + (Regurgitate only +key, not #key.)
- Regurgitate every x ms a key press until is release by user/remapper or another key is pressed.
 - Not just regurgitate, but sendinputs from remapping too
 + Mimics Windows repeat delay and repeat rate. No because apparently they are hardware-dependent.
+ Hint box border is broken for position history hints
+ It is possible to re-implement a zoom without using the Windows Magnifier:
 + https://stackoverflow.com/questions/63111438/implement-lens-magnification-to-magnify-the-windows-taskbar
   (it requires UIAccess)

+ move-to-last-selected-hint command?
- _{leftctrl} should be allowed release: _{leftctrl} +a -leftctrl +b
 - _{leftctrl} should only mean that leftctrl has to be pressed beforehand
 - Similarly ^{leftctrl}: ^{leftctrl} +a +leftctrl +b

- Hint box border: share (split 50/50) border only if there is another hint box next to it

+ Zoom
 + zoom-in=+leftshift, zoom-out commands
  + zoom.center=screen-center, mouse, last-selected-hint
  + zoom.increment-percent=4.0
 (+ zoom.max-percent, min-percent? min >= 1)
  - No zoom commands. Instead: hint2-2-mode.zoom.percent=5.0, .center=...
 - Always full screen
 - Show hints on top of zoomed window, with scaled coordinates and size
  + Also scale grid and indicator
 + smooth-zoom-enabled=true, smooth-zoom-velocity (like smooth-jump)
  + Expensive if hints need to be recomputed
   + Hide hints while animating and compute hints only once with the final zoom percent?
 + ZoomSynchronization mouse-and-zoom-center-unsynchronized, zoom-center-follows-mouse
  + Or: always follow mouse but only if it moves (otherwise stay on last-selected-hint)
   + No because doesn't work with smooth jump
   + Actually maybe because MagSetWindowSource is expensive and if we can not call it when
     moving the mouse, that is better.
- Font size, etc. should be decimals (because scaled by zoom): border width of 0.2: 0.2 * zoom(5.0) = 1px
- https://www.codeproject.com/Articles/42529/Outline-Text
 - https://www.codeproject.com/KB/GDI/Glow_and_Shadow_effects.aspx
- font-outline-thickness, outline-color, outline-blur=true, (offset x/y?)
- Space each hint key, font-spacing-percent=1.0 means spaced evenly across the whole cell
 - 0 means characters (hint keys) are drawn next to each other (first version: all characters at the cell center)
  - Save the width of each hint key
- logging.redact-keys=true
- Empty position history problem: add current position if empty
- hint.box-border-length=1 means one dot (of size box.thickness) as the border?
+ Clear hint cache when reloading config?
- Group hint keys (e.g. top left quadrant starts with Q, top right starts with W)
 - q w e r t a s d f g z x c v b y u i o p h j k l ; n m , . /
 - hint2-2: q w e r u i o p a s d f j k l ; z x c v m , . /
 - hint.grid-group-column-count=5, hint.grid-group-column-count=6
 - sub-grid-?
 - Increase row/col cell size until it can fit full sub-grids
  - No: draw partial sub-grid. If not enough hint keys, then switch to length 3 or 4
+ readme: hint styling showcase
+ Pre-load hint meshes on startup to avoid first time lag
 + Try caching each key drawing into a small int[][]?
 + Window hint grid: must clear draw cache if size == 10
- key-alias.hint1key.qwerty=..., key-alias.hint1key.azerty=...
 - Reload config file when layout changes?
 - Layout names: us-qwerty, uk-qwerty, italian-qwerty, french-azerty
 - Include defaults key-alias.hint1key, key-alias.hint1key.*, but if there is one
   key-alias.hint1key(.*) in the config file then ignore the defaults
   - Also include other key aliases: left/down/etc.
   - Remove the heading # character for each key-alias you want to modify.
- previous-mode-selected-hint
 - When switching mode, Map<List<Hint>, hintCenter==previous mode selected hint>
- WindowsFont that convert "Arial Bold Italic" to "Arial" + font style BoldItalic
- Divide hint cell into subcells with a sub grid style (box border thickness/length/color/opacity)
+ Click commmands that do both press and release?
 + normal-mode.reset-previous-hint-state=true
 + Goal: remove hint-xx-then-click modes, just have hint-xx- modes
 + timeout.only-if-idle: if hint not selected then not idling, otherwise hint is idling
 + Remove hint.swallow-hint-end-key-press? Always false
  + Remove mode-after-selection? Now becomes mode.to.normal-    mode=+hintkey
+ Should hint undo be a command/combo? hint1-mode.hint.undo=+backspace | +hh
 + Problem when pressing backspace when at least one hint key is selected:
   we do not want to go to another mode, but just perform the undo instead.
- Rename grid-row-oriented to layout-row-oriented
- hint-then-rightclick color
- Floating point inaccuracy issue
 - Make hint cell size independent of zoom: HintManager creates hints in full-screen unzoomed coordinates, then when
   a hint is selected, it uses the current zoom to determine the actual position of the hint
+ hint.screen-prefix-selection-keys=1 2 (default to none)
+ hint-area-filter.leftscreen=1920x1080
 + mode.hint.leftscreen.grid-cell-width
-- hint.select=^{leftalt} +hintkey -> feeds hint key press to HintManager, only if leftalt is not pressed
 + hint.selection-keys could be computed using the last (i.e. rightmost) moves of the hint.select multi-combo
 + That would allow holding modifier (e.g. +n) and a hintkey (e.g. j/k) to do a wheel command
 0- Problem with hint1-mode.to.hint2-mode=+hint1key
  0- Need to know that the combo triggers hint match. Roll back hint.mode-after-selection?
   -- Or, whenever SelectHintKey command (other than one that finalizes the hint), other commands are ignored?
   - Same for undo
   - HintManager.pollHintCommandSupercedesOtherCommands, SelectHintKey should be top priority command (executed first)
 -- hint.undo=+backspace, but problem with hint2.to.hint1=+backspace
  0- hint.break-combo-on-undo? No: hint2.hint.mode-after-noop-undo=hint1
  0- Allow hint.undo=backspace for backward compatibility
  0- Remove eat-unused-selection-keys
  0- Remove hint.mouse-movement? replace with mode.move-to-hint-grid-center=+hintkey | +backspace
    0- But how to move only when hint match? Next mode
    0- If move-to-hint-grid-center=+backspace, I may still not want to move if this also does a noop undo. Maybe =+hintkey-0 +backspace?
 -- Combo commands like selectHintKey should be inside .hint: .hint.select, .hint.undo
 + Instead of hint-then-l/m/r/click-mode, hint1-then-click-mode.hint.box-border-color._{rightbutton}=...
  (Combo precondition as part of the property key)
  + Same for hint1-mode.hint.mode-after-selection.leftctrl+leftshift=hint2-mode
- Self feed "fake" events to ComboWatcher for automation?
 - E.g. open hint, then select and click JI to focus Firefox address bar: +rightalt -rightalt +rightalt -rightalt +j -j +space +i -i -space
 https://github.com/babashka/babashka/blob/master/.github/workflows/build-windows.yml
 + SendInput KEYEVENTF_UNICODE for sending character or strings independently of active layout (e.g. %)

- ^{leftctrl f} as combo move to mean "release both in any order" = -leftctrl -f | -f -leftctrl
 - _{leftctrl f} as combo move means "press both in any order" = #leftctrl #f | #f #leftctrl
 - Or: {+leftctrl -f} as combo move set means "both move in any order" = +leftctrl -f | -f +leftctrl
  - +leftctrl {-leftctrl -f} = +leftctrl -leftctrl -f | +leftctrl -f -leftctrl
  - {+leftctrl +f} {-leftctrl -f} = +leftctrl +f -leftctrl -f | +f +leftctrl -leftctrl -f | +leftctrl +f -f -leftctrl | +f +leftctrl -f -leftctrl
  - Moves in brackets can be alias, and they will have to be expanded.

+ Complete priority for prefix-selected-font-size on viewport X (first non-null wins):
  Child's own explicit values (pre-pass, before parent extend):
    1. prefix-selected.X (child viewport)
    2. prefix-selected (child no-viewport)
    3. prefix default (child viewport)
    4. prefix default (child no-viewport)
    5. selected (child viewport)
    6. selected (child no-viewport)
    7. font-size (child viewport)
    8. font-size (child no-viewport)
    9. Same as 1-8 but for parent

++ Move for any key other than {a b c} ? Like an anti-alias that gets binded
 + !key or !alias ? (like boolean inversion)
 + Maybe could replace wait-ignore-all-except-{a b} with wait-ignore-all{!a !b} (or !{a b} ??)
 + +!a mean press any key other than a. Gets binded: +!a -!a is press-then-release-of-the-same-key-X
 + {!a !b} any-order subset or !{a b} (because I think we will never need {a !b} ??). None will be needed. Too complex

-- Only the last pressed key should be repeated: when user presses key, WindowsKeyboard should stop repeating?
- Missing a concept for saying, mid-combo, all of these keys get released (but not necessarily consecutively)?
- _{x} and leading ignore- are redundant?
- should {-alias} mean release all?
 - {+alias} would expand to {+a +b} = press all (in any order).
 - We do lose the alias binding (can't use alias in macro output) which is problematic. Maybe {+*alias} for expansion?
- Leading ignore applies everywhere? ~{a b c} +d +f means press d then f, and a b c can be pressed/released anytime in between d and f. No
- ignore-{arrowkeymodifier} +middlebutton ignore-{arrowkeymodifier} +anyarrowbasekey does not work because it should not match this (; is released):
 processKeyEventForCurrentMode ran in 4ms, mode = normal-mode, comboCount = 35, event = +i, currentlyPressedComboKeys = [i], comboPreparation = [+rightalt, -rightalt, +k, +l, -l, -k, +k, -k, +l, -l, +j, -j, +j, -j, +;, -;, +i], partOfComboSequence = true, mustBeEaten = true, commandsToRun = [StartMoveUp[], SwitchMode[modeName=arrowbasekey-mode]]
- Why ignore- is a word but _{} or ^{} (preconditions) are not?
   ~{*}-2000           # = ignore-all-2000
   ~{keys}-2000        # = ignore-{keys}-2000
   ~{!keys}-2000       # = ignore-all-except-{keys}-2000
 - mid-combo _{} and ^{}? No
 - What if #{*} (or #{keys}) applied to a group of moves made them noise-tolerant
   -  #{*} {-a -b}      # the ambient tolerance makes the MoveSet noise-tolerant
   - {-a -b #{*}} +c #{*} +d
   Or should #{*} before a {} group implicitly apply within it too? That feels inconsistent with the positional model though.
   I think keeping it strict — #{*} is always between MoveSets, and #{*} inside {} means noise-tolerant within the group — is the cleanest.
   {-a -b #{*}-0-500} = release a and b, any order, noise tolerated between them, but each noise gap must be within 500ms.
 - syntax: #{a b} = ignore a and b, #!{a b} = ignore all except a and b
 - opti: ignored events e.g. in {-a -b -c #{*}} should be removed from the growing preparation if they are not useful
- combo -key does not look at pressed keys: it will match even if there are pressed keys while releasing -key. What if combo starting with +key did not need _{any} either?
+ a should be a shorthand for +a -a, even in moveset groups: {a b} = {+a -a +b -b} ??
++ leftalt is treated specially in WindowsKeyboard. What about other keys? Maybe rightalt needs special treatment?
- Timeout for WindowsKeyboard.moveWaitingForKeyboardHookCallbackAcknowledgment: maybe 1 second? 5 ticks

+ I think the KeyMoveSet should be changed to allow not only +key -key #key but also "key" which would be "+key -key" (in order). so, "key'
 + would be a required TapComboMove and key? would be an optional TapComboMove. KeyMoveSet should be renamed to something else to reflect that.
 + and with alias1=a b, {*alias1? +c} should also accept +a +b +c -b -a (-a is after +a so it's fine).

++ shutdown hook: unpress keys pressed by macro?

+++ hint1-mode.hint.undo=backspace is not valid anymore as it expands to +backspace -backspace
++ Log which combos a preparation is part of e.g. normal-mode.start-move.down

- Combo-triggered property change?
 - oneshot-mode.indicator.idle.color=leftctrl -> #00FF80 | {leftctrl leftshift} -> #FFFF00
 - Longest combo match: in previous example, if both leftctrl and leftshft are tapped, then second combo runs
 - hint-mode.hint.box-border-color=#00FFFF | _{rightalt} -> #FFFFFF
  - #00FFFF is the value applied when none of the other combos match
  - When rightalt is released, it should go back (from #FFFFFF) to #00FFFF. How? Not clear for combo sequences
  - If all combos are precondition-only, then can go back to #00FFFF when none of the combos match.
  - If combos include sequences, then user needs to specify combo sequence to go back
    to initial property value, and the initial value alone jis only used on mode change
 - Need a way to update all properties while keeping the immutable approach.
  - Changing the property should kind of be like if the mode was changed and the mode has a new property value.
  - Maybe we should see these property changes as a temporary mode tweak?
    i.e. a new Mode() instance is created with the new value(s) and sent to the ModeController.
   - ComboWatcher would keep track of the temporarily active mode properties
 - Mostly useful for indicator properties and hint properties
 + Cannot currently have multi-combo -> mutation
 + Maybe need a combo-alias if we want to use the same combo multiple times for many properties?
 - In typing mode: typing-loop-mode.indicator.idle.label-text= | #typingkeys -> typingkeys | -typingkeys ->
   which binds typingkeys and use that as property value, works only for properties that accept unrestricted strings
   - E.g. if typingkeys is leftalt then new property value is Key.hintLabel(), i.e. ⎇. hintLabel should be renamed to something more generic, maybe label.
   - alias expansion should also work: {*typingkeys} -> typingkeys: if user tap a then b, then new property value is ab
 - killing shorter combos is not right?
 - .noop should have multiple named properties, like macros? still allow no-name noop: zoom-mode.noop=+middlebutton zoom-mode.noop.eatx=+x
 - Problem with shift + '
 - After ANY event (press or release) or time condition expiry, if an eating combo can no longer match the preparation, immediately detect
    it as dead and regurgitate affected keys.

- Toggle mutation if mutation already active when completing mutation combo
- boolean variables in config, or mutable combos? to avoid lclick/rclick/mclick/nm modes with tapping. Need to solve the | -> precedence problem.
 (would require to get rid of the click-after- modes: is that possible?)
  - set/unset variables? .set-var.ishintnomove=+/ .unset-var.ishintnomove=-/
  - Used in combos as variable precondition: _{ishintmove !ishintlclick}, or _{ishintlclick | ishintrclick | ishintmclick}
+ Get rid of lastHintCommandSupercedesOtherCommands. This is too much magic.

+ Add transition anim for grid, and labels (with a size list per level, e.g. font-sizes=72 32 8), and background color (like hints)
 + Label text = last press key of e.g. screen-grid-mode.shrink-grid.down=^{leftshift leftctrl} +down
  + Should reuse HintLabel or even HintBox? Want same features: color, shadow, outline, etc.
  + Unlike hint transition animation, grid animation should draw grid with an animated shrinking width/height. Not screenshot-based anim